# OTAU App — переменные окружения (прототип без секретов)

# Необязательно: свой домен для Open Graph / metadataBase (иначе на Vercel берётся https://$VERCEL_URL)
# NEXT_PUBLIC_SITE_URL=https://your-domain.kz

# На Vercel задаются автоматически: VERCEL_URL, NODE_ENV
